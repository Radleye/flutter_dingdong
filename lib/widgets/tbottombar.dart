import 'package:flutter/material.dart';

/*
class TheBottomBar extends StatefulWidget {
  @override
  _TheBottomBarState createState() => _TheBottomBarState();
}

class _TheBottomBarState extends State<TheBottomBar> {

   // final List pages=[ShopScreen(),]

  int _selectedIndex = 0;

  void _onItemTapped(int index) {
    setState(() {
      _selectedIndex = index;
    });
  }

  /*
  @override
  Widget build(BuildContext context) {
    return BottomNavigationBar(
      items: <BottomNavigationBarItem>[
        BottomNavigationBarItem(
          title: Text('商铺'),
          icon: Icon(MyIcon.shop),
        ),
        BottomNavigationBarItem(
          title: Text('我的订单'),
          icon: Icon(MyIcon.bag),
        ),
        BottomNavigationBarItem(
          title: Text('发布'),
          icon: Icon(MyIcon.add),
        ),
        BottomNavigationBarItem(
          title: Text('消息'),
          icon: Icon(MyIcon.message),
        ),
        BottomNavigationBarItem(
          title: Text('个人'),
          icon: Icon(MyIcon.profile),
        ),
      ],
      currentIndex: _selectedIndex,
      selectedItemColor: Colors.blue,
      onTap: _onItemTapped,
      unselectedItemColor: Colors.grey,
    );
  }
}
*/*/